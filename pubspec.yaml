name: flutter_base_logger
description: "A Flutter logger"
version: 0.0.1
homepage: https://github.com/zenganiu/flutter_base_logger

environment:
  sdk: ^3.8.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0